Algoritmo "Funcoes_Matematicas"

Var

a, b, c, d, x, y, z : real

Inicio

x <- 2.0
y <- 3.0
z <- -7.0

a <- Exp(x, y)
b <- RaizQ(y)
c <- Pi
d <- Abs(z)

EscrevaL(a)
EscrevaL(b)
EscrevaL(c)
EscrevaL(d)

Fimalgoritmo